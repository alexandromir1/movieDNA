"use client";

import { ProfileOverview } from "@/components/profile/ProfileOverview";

/** @deprecated use ProfileOverview — kept so old imports compile */
export function StatsOverview() {
  return <ProfileOverview />;
}
